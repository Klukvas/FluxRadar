// The version of the one-line notice under the Action Plan's Generate and
// Regenerate buttons. The web bundle has no workspace dependencies, so the API
// declares the same value in `@fluxradar/contracts`; its contract test reads
// this file and fails when the two drift apart.
export const ACTION_PLAN_NOTICE_VERSION = 'action-plan-notice-v1';
