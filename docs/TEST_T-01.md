# Отчёт тестирования T-01 — Скелет pnpm-монорепо

**Дата:** 2026-09-03  
**Проект:** FluxRadar  
**Задача:** T-01 — инициализация pnpm-монорепо

---

## Результаты по командам

| # | Команда | Результат | Примечание |
|---|---------|-----------|------------|
| 1 | `pnpm lint` | **ПРОЙДЕНО** | ESLint завершился без ошибок и предупреждений |
| 2 | `pnpm typecheck` | **ПРОЙДЕНО** | `tsc --noEmit` прошёл во всех 10 пакетах/приложениях |
| 3 | `pnpm -r build` | **ПРОЙДЕНО** | Все пакеты собраны; Vite-сборка web выдала `dist/index.html` + `dist/assets/index-*.js` (190 КБ) |
| 4 | `pnpm test` | **ПРОЙДЕНО** | 9 тест-файлов (1 на каждый пакет/app), все прошли; `apps/web` — нет тестов, завершился с кодом 0 (`--passWithNoTests`) |
| 5 | `pnpm --filter @fluxradar/web build` | **ПРОЙДЕНО** | Vite v8.2.2, 15 модулей, сборка за 57 мс |
| 6 | Проверка `dist/index.js` | **ПРОЙДЕНО** | Все 9 артефактов присутствуют: `apps/api/dist/index.js` + `packages/{ai,contracts,crawler,export,fingerprint,rules,safe-fetch,scoring}/dist/index.js` |

---

## Детали

- **Workspace:** 11 проектов в workspace, 10 активных (root исключён из рекурсивного запуска).
- **Node.js / pnpm:** engines требует `>=24`, packageManager `pnpm@10.11.0`.
- **TypeScript:** версия `^6.0.3`, все проекты используют `tsconfig.base.json` из корня.
- **Vite-сборка web:** выходной бандл `190.59 kB` (gzip: `60.04 kB`), время сборки ≤ 120 мс.
- **Тесты:** каждый пакет содержит ровно 1 smoke-тест (`1 passed`), `apps/web` тестов не имеет — завершается корректно.

---

## Итоговый вердикт

**PASS** — все 6 проверок пройдены без ошибок.
